DATABASE_URL=postgresql://inwards:replace-me@localhost:5432/inwards
APP_URL=http://localhost:3100
# For local integration tests only; never point this at a live database.
TEST_DATABASE_URL=postgresql://inwards:replace-me@localhost:5432/inwards_test
# On first deployment, user:bootstrap creates this account only if no users exist.
# Also used by npm run user:create to add further staff accounts.
CREATE_USERNAME=admin
CREATE_DISPLAY_NAME=Warehouse Admin
CREATE_PASSWORD=
